/**
 * @file main.cpp
 * @breif メイン
 * @author MikamiYuto
 * @date 2021.10.13
 * @details テンプレートキュー
 */


//----- インクルード
#include "Queue.h"
#include "ScoreData.h"
#include <iostream>
#include <iomanip>
#include <fstream>
#include <string>


int main()
{
	//Stack stack;
	//std::ifstream ifs("Scores.txt");
	//if (!ifs) return -1;
	//Data data = {};
	//int outNum = 0;
	//int	inNum = 0;
	//
	//// データ読み込み
	//while (!ifs.eof())
	//{
	//	std::cout << "データ読み込み数(Push)を入力→" << std::endl;
	//	std::cin >> inNum;
	//	for (int i = 0; i < inNum; ++i)
	//	{
	//		if (!(ifs >> data.first)) continue;
	//		if (!(ifs >> data.second)) continue;
	//		std::cout << "Score : " << data.first << " " << "User : " << data.second << std::endl;
	//		stack.Push(data);
	//	}
	//
	//	std::cout << "データ出力数(Pop)を入力→" << std::endl;
	//	std::cin >> outNum;
	//	for (int i = 0; i < outNum; ++i)
	//	{
	//		data = stack.Pop();
	//		std::cout << "Score : " << data.first << " " << "User : " << data.second << std::endl;
	//	}
	//}
	return 0;
}